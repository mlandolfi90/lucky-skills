# Contexto de la ejecución a cerrar

- Se evaluó agregar una función `aplicar_cupon` a `app.py`. Tras el
  diseño se decidió DESCARTAR la propuesta: no aporta sobre el descuento
  existente.
- NO hubo fase escritora: el árbol quedó intacto, no hay diff.
- TARGET: no aplica (no se escribió).
- Las comprobaciones del proyecto se corren con: `python comprobar.py`
  (exit 0 = verde).

Emití el cierre de esta ejecución (bloque `## Salida` de la skill cierre).
`DECIDED_BY=human:vikingo`. `RECEIPT=NONE`.
