.venv/
__pycache__/
*.egg-info/
.pytest_cache/
# El comodin adelante NO es cosmetico: el nombre del archivo lleva el prefijo
# del MCP, asi que `auditoria-*.jsonl` no le pega a `gns3-mcp-auditoria-...`.
# Olvidarlo commitea material sensible.
*auditoria-*.jsonl
