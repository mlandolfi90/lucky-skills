{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lucky-skills/registry@1",
  "title": "lucky-skills registry (manifiesto del loader cargar)",
  "description": "Valida la FORMA del registry.json. La CONFIANZA la dan minisign -V (firma del registry) + sha256 -c por SKILL.md, ejecutados por codigo externo. Este schema solo evita un manifiesto malformado; lo corre scripts/forjar-release.sh ANTES de firmar (defensa en profundidad).",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "tag", "commit", "raw_base", "skills"],
  "properties": {
    "schema": {
      "type": "string",
      "description": "Pin de version del formato del manifiesto (ej. lucky-skills/registry@1)."
    },
    "tag": {
      "type": "string",
      "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+(-rc[0-9]+)?$",
      "description": "Tag git (etiqueta humana, MUTABLE: git -f lo mueve). El pin real es 'commit'."
    },
    "commit": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$",
      "description": "SHA40 del commit del release. ANCLA INMUTABLE: el fetcher fetchea/verifica raw@<commit>. Mover el tag no cambia el commit firmado."
    },
    "raw_base": {
      "type": "string",
      "description": "Token literal '${SKILLS_REGISTRY_URL}' (zero-leak): el host/dominio real lo provee el entorno (Infisical) en runtime. Prohibido un host/IP concreto aca."
    },
    "generated": {
      "type": "string",
      "description": "Fecha ISO de generacion por el script de release (auditoria, no es ancla de confianza)."
    },
    "note": {
      "type": "string",
      "description": "Nota humana sobre el modelo de verificacion. No es campo de confianza."
    },
    "skills": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/skill" }
    }
  },
  "$defs": {
    "skill": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "triggers", "kind", "loadable_as_data", "requires_runtime", "requires_tools", "path", "sha256"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Nombre canonico; == frontmatter name (minusculas)."
        },
        "triggers": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 },
          "description": "Frases/alias de invocacion que el resolver-de-catalogo matchea contra el pedido del humano."
        },
        "kind": {
          "type": "string",
          "enum": ["method", "runtime"],
          "description": "method = puro-texto sin runtime declarado; runtime = declara hook/allowed-tools de escritura/auto-trigger/disable-invocation. NO determina por si solo loadable_as_data (una skill method puede depender de tools -> requires_tools)."
        },
        "loadable_as_data": {
          "type": "boolean",
          "description": "true SOLO si la skill es puro-texto-autocontenido (sin requires_runtime y sin requires_tools). El loader la inyecta como dato verificado. false => fast-path de install (jamas castrada)."
        },
        "requires_runtime": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": ["hooks", "allowed-tools-extra", "auto-trigger", "disable-invocation"]
          },
          "description": "Banderas de harness que el texto-en-historial NO puede reproducir. No-vacio => loadable_as_data debe ser false."
        },
        "requires_tools": {
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string",
            "enum": ["Bash", "Write", "Edit", "MultiEdit", "Agent", "SendMessage", "TodoWrite"]
          },
          "description": "Tools de ejecucion de las que DEPENDE el metodo de la skill. Si esta presente, inyectada como dato seria prosa inerte (metodo castrado) => loadable_as_data debe ser false. Separado de requires_runtime: 'depende-de-ejecutar-tools' != 'depende-de-harness'."
        },
        "path": {
          "type": "string",
          "pattern": "^[A-Za-z0-9._/-]+\\.md$",
          "description": "Ruta RELATIVA al raw del repo (zero-leak). El loader compone <raw_base>/<commit>/<path>. Sin esquema ni host: prohibido absolute URL."
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Hash esperado del SKILL.md en ese commit (sobre bytes LF). Lo verifica sha256 -c externo; el modelo NUNCA lo computa ni lo transcribe (el fetcher lo extrae del registry firmado por si mismo)."
        }
      },
      "allOf": [
        {
          "if": {
            "anyOf": [
              { "properties": { "requires_runtime": { "minItems": 1 } }, "required": ["requires_runtime"] },
              { "properties": { "requires_tools":   { "minItems": 1 } }, "required": ["requires_tools"] }
            ]
          },
          "then": {
            "properties": { "loadable_as_data": { "const": false } }
          }
        }
      ]
    }
  }
}
